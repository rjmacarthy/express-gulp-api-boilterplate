extends layout

block content
	h2= title
	h3 Available routes
	ul
		each route in routes
			li 
				p #{route.path}
				p 
					ul 
						each allowed, key in route.methods
							li 
								p #{key} - #{allowed}